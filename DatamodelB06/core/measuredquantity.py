from enum import Enum


class MeasuredQuantity(Enum):
    EE = "ee"
    TON = "TON"
    TOF = "TOF"
    YIELD = "yield"
    TIME = "time"
    TEMPERATURE = "temperature"
    CONCENTRATION = "concentration"
